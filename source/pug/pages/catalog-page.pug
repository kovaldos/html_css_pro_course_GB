extends ../components/base/layout

block variables
  - const title = "BRAND" //- на страницах кроме главной используйте О нас — ProjectName
  - const wrapperMods = ""
  - const hasHeader = true
  - const headerMods = ""
  - const hasFooter = true
  - const footerMods = "catalog-page"
  - const currentUrl = "catalog-page.html" //- для остальных страниц пишем пустую строку или индекс активного headerLinks

block main
  main
    section.catalog-page
      +page-head("catalog-page")
      +filter()
      +cards("catalog-page")
      +pagination(1, 20)

block modals
